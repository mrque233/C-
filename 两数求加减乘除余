#include <stdio.h>
#include<math.h>
int main()

{

int x,y,a,b,c,d,e;

scanf("%d%d",&x,&y);

a=x+y;
b=x-y;
c=x*y;
d=x/y;
e=x%y;

printf("sum = %d difference = %d, product = %d, quotient = %d, remainder=%d",a,b,c,d,e);

return 0;
